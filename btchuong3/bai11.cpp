#include <bits/stdc++.h>
using namespace std;

int main() {
    double a, b;
    cin >> a >> b;

    if (a > 0 && b > 0)
        cout << a * b;
    else
        cout << "Du lieu nhap khong dung";

    return 0;
}